<% provide(:title, "Sign Up") %>
<div class="container">
	<div class="row">
		<div class="col-md-6 col-md-offset-3">
			<%= form_for(@user) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Join the hooligans!" %>
			<% end %>
		</div>
	</div>
</div>
